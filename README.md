# Stypz-Shortner
# HOW TO USE

# TERMUX
apt update -y && pkg install python -y
pkg install git -y
git clone https://github.com/snitchbossdotcom/Stypz-Shortner
cd Stypz-Shortener
pkg install python-pip -y
pip install -r requirements.txt
python main.py

# KALI LINUX && UBUNTU && DEBIAN
apt update -y && apt install python -y
apt install git -y
git clone https://github.com/snitchbossdotcom/Stypz-Shortner
cd Stypz-Shortener
apt install python-pip -y
pip install -r requirements.txt
python main.py

# I USE ARCH BTW 🤓

sudo pacman -Syu 
sudo pacman -S python
sudo pacman -S git
git clone https://github.com/snitchbossdotcom/Stypz-Shortner
cd Stypz-Shortener
sudo pacman -S python-pip 
pip install -r requirements.txt
python main.py
